<configure
    xmlns="http://namespaces.zope.org/zope"
    xmlns:browser="http://namespaces.zope.org/browser"
    xmlns:plone="http://namespaces.plone.org/plone"
    i18n_domain="plone.palette"
    >

  <!-- Set overrides folder for Just-a-Bunch-Of-Templates product -->
  <include
      package="z3c.jbot"
      file="meta.zcml"
      />
  <browser:jbot
      directory="overrides"
      layer="plone.palette.interfaces.IPaletteLayer"
      />

  <!-- Publish static files at /++plone++plone.palette/ -->
  <plone:static
      directory="static"
      name="plone.palette"
      type="plone"
      />

  <!-- @@palette-customizer: standalone view (also used by viewlet template) -->
  <browser:page
      name="palette-customizer"
      for="*"
      class=".customizer.CustomizerView"
      permission="cmf.ManagePortal"
      layer="plone.palette.interfaces.IPaletteLayer"
      />

  <!-- @@palette-save: POST endpoint, returns JSON -->
  <browser:page
      name="palette-save"
      for="*"
      class=".customizer.SaveCustomizerView"
      permission="cmf.ManagePortal"
      layer="plone.palette.interfaces.IPaletteLayer"
      />

  <!-- Toolbar entry "Theming" + the customizer offcanvas it opens.
       plone.toolbar is the IToolbar viewlet manager rendered by
       plone.app.layout's toolbar.pt, inside <ul class="plone-toolbar-main">. -->
  <browser:viewlet
      name="plone.palette.toolbar"
      manager="plone.app.layout.viewlets.interfaces.IToolbar"
      class=".customizer.CustomizerToolbarViewlet"
      permission="cmf.ManagePortal"
      layer="plone.palette.interfaces.IPaletteLayer"
      />

  <!-- Viewlet: injects offcanvas HTML into IPortalTop for managers -->
  <browser:viewlet
      name="plone.palette.customizer"
      manager="plone.app.layout.viewlets.interfaces.IPortalTop"
      class=".customizer.CustomizerViewlet"
      permission="cmf.ManagePortal"
      layer="plone.palette.interfaces.IPaletteLayer"
      />

</configure>
